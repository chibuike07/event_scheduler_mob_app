import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const add_event = () => {
  return (
    <View>
      <Text>add add_event</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default add_event;
